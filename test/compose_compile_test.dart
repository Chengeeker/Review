import 'package:flutter_test/flutter_test.dart';
import 'package:review/features/compose/presentation/compose_tweet_page.dart';

void main() {
  test('compose page compiles with publish integrations', () {
    expect(ComposeTweetPage, isNotNull);
  });
}
